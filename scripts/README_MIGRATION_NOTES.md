# Legacy MongoDB Scripts

The following scripts were part of the old MongoDB migration path and are no longer used by the app:

- `scripts/check-mongo.js`
- `scripts/create-admin-verify.js`
- `scripts/test-mongo.js`

These files remain for historical reference only and can be deleted once the Supabase migration is fully validated.
